# a config file for the csv files.
DATASET_DIR = 'datasets/ml-latest-large'
CSV_FILES = {
    'movies': f'{DATASET_DIR}/movies.csv',
    'ratings': f'{DATASET_DIR}/ratings.csv',
    'tags': f'{DATASET_DIR}/tags.csv'
}